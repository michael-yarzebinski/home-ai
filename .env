# =============================================================================
# AI Configuration (NEW)
# =============================================================================
AI_PROVIDER=local                    # Change to "cloud" when ready

# Local Ollama (recommended for now)
LOCAL_AI_URL=http://localhost:11434/api/generate
LOCAL_MODEL=qwen2.5:7b               # Try qwen3:8b once you pull it

# Cloud settings (only used if AI_PROVIDER=cloud)
CLOUD_AI_URL=https://api.openai.com/v1/chat/completions
CLOUD_AI_API_KEY=sk-...              # ← Fill this in when switching to cloud
CLOUD_MODEL=gpt-4o-mini

# Shared
AI_TEMPERATURE=0.0

# =============================================================================
# Home Assistant (NEW - needed for device queries + discovery)
# =============================================================================
HA_URL=http://localhost:8123
HA_LONG_LIVED_TOKEN=eyJ...           # Create this in HA → Profile → Long-Lived Access Tokens

# =============================================================================
# Existing settings (unchanged)
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=aiadmin
DB_PASSWORD=aihome_secure_2026
DB_NAME=aihome

SERVER_PORT=3000
NODE_ENV=development
PLATFORM=windows

WEBHOOK_SECRET=change_this_to_a_strong_secret_please
WEATHER_ZIP_CODE=90210